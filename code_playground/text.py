#!/usr/bin/python3


words = ['cat', 'dog', 'dragon', 'horse']


for word in words:
    w = len(word)
    print(f"{word} contains {w} alphabets")

names = ['Kingsley', 'Mercy', 'Emeka', 'Henry']

for name in range(len(names)):
    print(name, names[name]) 
